interface:
  display_name: "Mathodology Agent Pipeline"
  short_description: "Worker pipeline and agent guidance"
  default_prompt: "Use $mathodology-agent-pipeline to change the worker agent pipeline safely."
